# Xournal++ Radial Menu - Python Dependencies

# GUI Framework (PyQt5 - sem dependência de OpenGL, funciona em qualquer ambiente)
# Nota: PyQt5 5.15.2 é a última versão estável com wheels para todas as plataformas
# Versões mais recentes (5.15.9+) têm problemas com wheels no Windows
PyQt5==5.15.2

# Input handling
evdev>=1.7.0; sys_platform == 'linux'
python-xlib>=0.33; sys_platform == 'linux'
pynput>=1.7.6

# Window automation
pyautogui>=0.9.54
pygetwindow>=0.0.9; sys_platform != 'darwin'

# Configuration
pyyaml>=6.0.1

# Build tools (dev)
pyinstaller>=6.0.0
