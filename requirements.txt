# Xournal++ Radial Menu - Python Dependencies

# GUI Framework (PyQt6 - sem dependência de OpenGL, funciona em qualquer ambiente)
# PyQt6 tem wheels para Python 3.9-3.13 em Windows/Linux/macOS
PyQt6>=6.4.0

# Input handling
evdev>=1.7.0; sys_platform == 'linux'
python-xlib>=0.33; sys_platform == 'linux'
pynput>=1.7.6

# Window automation
pyautogui>=0.9.54
pygetwindow>=0.0.9; sys_platform != 'darwin'

# Configuration
pyyaml>=6.0.1

# Build tools (dev)
pyinstaller>=6.0.0
